# SPDX-FileCopyrightText: 2026 Suguru Hirahara
# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Verify Outline systemd service
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Wait for Outline service to become active (up to 180s)
      ansible.builtin.systemd:
        name: outline.service
      register: outline_service
      until: outline_service.status.ActiveState == 'active'
      retries: 36
      delay: 5

    - name: Assert Outline service is active
      ansible.builtin.assert:
        that: outline_service.status.ActiveState == 'active'
        fail_msg: "outline.service not active ({{ outline_service.status.ActiveState }})"
        success_msg: "outline.service is active"

    - name: Retrieve Outline service log for debugging
      when: outline_debug_service_enabled | bool
      ansible.builtin.command:
        cmd: journalctl -xeu outline.service
      changed_when: false

    - name: Wait for Outline HTTP endpoint to respond (up to 10m)
      ansible.builtin.uri:
        url: "http://localhost:3000/"
        method: GET
        status_code: [200, 301, 302]
        follow_redirects: none
      register: outline_endpoint
      until: outline_endpoint.status in [200, 301, 302]
      retries: 120
      delay: 5

    - name: Assert Outline HTTP endpoint responds
      ansible.builtin.assert:
        that: outline_endpoint.status in [200, 301, 302]
        fail_msg: "Outline did not respond as expected ({{ outline_endpoint.status | default('no response') }})"
        success_msg: "Outline responded with {{ outline_endpoint.status }}"

    - name: Show Outline systemd service status summary
      ansible.builtin.debug:
        msg:
          - "Outline systemd: {{ outline_service.status.ActiveState }}"
          - "Outline HTTP: {{ outline_endpoint.status }}"
